apiVersion: batch/v1
kind: Job
metadata:
  name: ediff-sota
  labels:
    app.kubernetes.io/name: ediff
    app.kubernetes.io/component: sota-validation
    app.kubernetes.io/part-of: ediff
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ediff
        app.kubernetes.io/component: sota-validation
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ediff
          image: ediff:latest
          imagePullPolicy: IfNotPresent
          workingDir: /app
          args:
            - bash
            - sota.sh
            - --regenerate-datasets
            - --seeds
            - 11,17,23,31,37,43,53,61
          env:
            - name: EDIFF_ARTIFACT_DIR
              value: /artifacts
            - name: EDIFF_ARCHIVE_DATASETS
              value: "0"
            - name: OMP_NUM_THREADS
              value: "1"
            - name: OPENBLAS_NUM_THREADS
              value: "1"
            - name: MKL_NUM_THREADS
              value: "1"
          resources:
            requests:
              cpu: "4"
              memory: 8Gi
            limits:
              cpu: "8"
              memory: 16Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: artifacts
              mountPath: /artifacts
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: ediff-artifacts
